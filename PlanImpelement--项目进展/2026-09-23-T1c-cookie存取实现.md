# 2026-09-23 T1c：cookie 存取实现

- **日期**：2026-09-23
- **改了什么**：填充 pageplay/cookies.py 全部 5 个函数（has_login_state / filter_by_domain / save_snapshot / load_snapshot / export_json），签名与契约逐字一致；新增 tests/test_cookies.py 共 13 支单测（tmp_path 隔离，不碰真实 HOME）。
- **为什么**：T1c 任务书要求实现登录态判定（先 filter_by_domain 再查标记）、子串域过滤、state.json 快照写读（schema 四键 + 0600 权限，chmod 失败静默跳过）、缺快照报 FileNotFoundError 带 "先 pageplay login <站点名>" 指引、导出仅 name/value/domain/expires 四键精简 JSON（ensure_ascii=False）。
- **影响**：session.py（T1d）的 refresh_snapshot/export_cookies 可直接依赖本模块；filter_by_domain 子串语义为 `domain in cookie["domain"]`，空/缺 domain 字段的 cookie 不命中但不崩。测试结果：tests/test_cookies.py 13 passed；全量 26 passed。

