# T1a 日志+护栏（coder-A）

- 日期：2026-09-23
- 改了什么：
  - `pageplay/logging_setup.py`（42 行）：空壳填成实现。setup_logging() 配 root logger，stderr 输出；verbose→DEBUG 否则 INFO；log_file 给定时加 FileHandler（自动建父目录，utf-8）；格式 `%(asctime)s %(levelname)-7s %(name)s:%(lineno)d %(message)s`。幂等策略选"清 root handler 再挂载"（docstring 已写明选型理由：CLI 需让第三方库日志汇入同一配置）。
  - `pageplay/guard.py`（65 行）：Guard 填成实现。wait() = curfew 检查（本地时间，[起,止) 左闭右开，命中且未 bypass 抛 RuntimeError）+ random.uniform 延时 + time.sleep；check_response() = 命中任一 risk_keywords 抛 RiskTriggered（消息带命中关键词）。新增关键字参数 bypass_curfew（默认 False，不破坏既有签名）。sycm 的 "618" 豁免未搬。
  - `tests/test_logging_setup.py`（100 行）/ `tests/test_guard.py`（116 行）：级别切换、文件落地与格式（正则验时间/模块:行号）、handler 组成、幂等不翻倍；延时区间（默认+自定义，50 次采样）、六关键词命中、无辜文本放行、宵禁边界 hour=0/1/5/6、bypass_curfew、curfew=None。全程 monkeypatch 打桩（time.sleep 与 datetime.now），不真 sleep；autouse fixture 恢复 root 日志状态不污染其他测试。
- 为什么：T1a 任务书（设计文档 §4 guard.py/logging_setup.py 契约节），骨架空壳填实现。
- 影响：仅本四个文件；接口契约未改（bypass_curfew 是任务书批准的新增关键字参数）。全量 pytest 50 passed，T0 冒烟无回归。主 agent 统一 git 提交。
