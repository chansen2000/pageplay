# T18：GUI 向导式重构（按使用顺序组织界面）+ 预览层拆分 gui_preview.py

日期：2026-09-25
分支基线：git 5774c65（v0.7.2，全量 340 passed 实测复跑确认 154.36s）
改动文件：`pageplay/gui.py`（498 → 431 行）、`pageplay/gui_preview.py`（新建 138 行）、`tests/test_gui.py`（259 → 286 行）；引擎文件零触碰（git status 仅上述三个）。

## 为什么改

11 钮平铺没人看得懂顺序（sheng 拍板"按使用顺序组织界面"）。同时 gui.py 498 行贴死 500 上限（红线 #26），上一条 v0.7-B 进展已预告"preview 数据层拆分"提优候选，本任务一并落地。

## 改了什么

### 1. 拆分：预览层整体搬入 pageplay/gui_preview.py（新建，138 行）

- 原样搬入 5 个预览符号（gui.py:32-34 回导，旧 import 路径全部不变）：
  - `latest_csv_output`（gui_preview.py:27）
  - `load_csv_for_preview`（gui_preview.py:66）
  - `should_preview`（gui_preview.py:60）
  - `_count_runs`（gui_preview.py:53）
  - `_runs_jsonl_path`（gui_preview.py:20，预览闸门专用基建，随层走）
- `App._open_preview` 搬出类 → 模块级 `open_preview_window(root, csv_path, columns, rows, truncated, preview_rows)`（gui_preview.py:101）：`self.root`→`root`、`self.PREVIEW_ROWS`→`preview_rows` 两处参数化，函数体逐行原样；gui.py 侧 `App._open_preview` 留 5 行薄委托保持旧方法名（gui.py:401）。
- 既有测试**零改动为证**：`from pageplay.gui import latest_csv_output, ...`、`pageplay.gui._count_runs` 逐字未动（git diff 证实既有 23 个测试函数无一字节变化）。

### 2. 向导式布局：App._build_ui 重写（gui.py:150-227），行为逻辑零变化

四段 LabelFrame 自上而下 = 使用顺序：

1. **第 1 步：登录网站（每个网站只需一次）**（gui.py:161-170）：站点/网址 Entry + [① 登录] + [验活]
2. **第 2 步：抓数据（两条路选一条）**（gui.py:172-201）：两行说明文字（"2A 临时抓一页：打开窗口 → 逛到目标页 → 取当前页"；"2B 反复自动抓：录制 → 逛+按P框选 → 关窗起名"）+ 控件两行：[打开窗口] [③ 取当前页] [② 录制] [④ 重放] / 名称 Entry + 流程/recipe 下拉 + 刷新 + 无头重放
3. **第 3 步：看结果**（gui.py:203-209）：[结果] [流程列表] [recipe列表] [导出]
4. **管理（低频）**（gui.py:211-214）：[关闭守护]
- 底部状态栏 + 取消钮 + 日志窗照旧（gui.py:216-227）；窗口 780x520 → 780x680 适配四段高度（gui.py:425）。
- 按钮序号 ①②③④ 与 op 映射不变（`add_button` 统一 `lambda op=op: self._launch(op)`，gui.py:154-159）；build_command 一字未动。
- `_launch/_pump/_poll/_on_done/_cancel/_escalate/_set_running/_log/refresh_names/_fetch_names/_on_names/_maybe_preview/_on_close` 逐字保留；`_set_running` 本就遍历 `self._buttons.values()`，新布局 12 钮全部登记（含 刷新），互斥覆盖面自动齐全。
- 说明：刷新钮（下拉刷新）与无头重放勾选框是既有功能控件，任务书步骤清单未列但"行为逻辑零变化"要求保留，分别摆进第 2 步下拉旁 / 重放所在行。

### 3. 测试（tests/test_gui.py，23 → 25 个）

- 既有 23 个零改动全绿。
- 新增 2 个：
  - `test_gui_source_keeps_wizard_layout_markers`（test_gui.py:266）：读 gui.py 源码静态断言含 "第 1 步/第 2 步/第 3 步/管理（低频）/① 登录/② 录制/③ 取当前页/④ 重放/2A 临时抓一页/2B 反复自动抓" 十个标记，防重构回退成平铺。
  - `test_preview_code_lives_in_gui_preview_and_reexported`（test_gui.py:276）：预览函数实体在 gui_preview 且 gui 回导**同一对象**（`is` 断言防拷贝分叉）。

## 实测证据

- 全量 pytest 连跑 2 次（代码冻结后）：**342 passed in 150.66s / 342 passed in 152.59s**（340 基线 + 2 新增；基线 340 passed in 154.36s 已先实测）。
- test_gui.py 单跑：25 passed in 1.23s。
- 真 Tk 冒烟（临时脚本，未入库）：App 可构建；四段 LabelFrame 标题按序就位；12 钮登记齐全；`_set_running` 全钮置灰/恢复与取消钮联动正常；`_open_preview` 薄委托能建"数据预览" Toplevel。
- import 零副作用复验：`import pageplay.gui` / `import pageplay.gui_preview` 不建 Tk root（main() 才起）。
- `pageplay-gui = "pageplay.gui:main"` 入口仍在（pyproject.toml:16）。
- 行数：gui.py 431 / gui_preview.py 138 / test_gui.py 286，全部 ≤500（红线 #26）。

## 影响与边界

- 用户可见变化只有布局与按钮文字（带序号）；命令、运行模型、预览触发、取消收尾全链路行为不变。
- `open_preview_window` 与 `App._open_preview` 双入口并存：前者是实现，后者保旧名；后续若有人直接调 App 方法不受影响。
