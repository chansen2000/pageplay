# 2026-09-23 T5：统一站点解析（修 sheng 真机测出的 doctor 贴 URL Bug）

- **日期**：2026-09-23
- **改了什么**：
  - `pageplay/cli.py`：
    - 删 `_resolve_saved`（内置优先的独立解析路径）与旧 `_resolve_site_or_report` 的 login_url 分支；
    - 新增 `_saved_site_names()`（cli.py:45，已存站点清单，供报错列出）、`_load_saved_preset()`（cli.py:53，meta.json 重建预设的唯一实现）、`_resolve_target()`（cli.py:87，非 login 命令统一解析入口：`parse_target` 推导名 → 已存 meta 优先 → 内置预设 → 报错）；
    - `_resolve_site_or_report()`（cli.py:113）改为薄包装：调 `_resolve_target`，捕获 KeyError/ValueError 打人话返回 None；doctor/export/open/forget 四个命令函数体零改动；
    - 报错文案换可执行指引：`站点 'x' 还没有登录记录；先运行 pageplay login <原样回显输入>。已存站点：<清单>；内置：sycm, taobao`，删除误导的 --url 提示（--url 参数仍在 login 上，未动）；
    - doctor/export/open/forget 的 help 从"站点名"改为"站点名或网址/域名"；
    - 模块 docstring 同步更新解析顺序说明。
  - `tests/test_cli.py`：新增 `_write_saved_site` helper + 7 个测试（贴 URL 访问已存站 doctor/export、已存优先于内置、贴内置 URL 未登录走内置、预设名不回归、未登录报错含 login 指引与已存清单且无 --url、forget 贴 URL）；旧 `test_unknown_site_name_returns_one` 调整为 `test_unsaved_site_name_returns_one_with_login_hint`（断言新文案）。
- **为什么**：T4 只把 parse_target 接进 login，doctor/export/open/forget 仍走旧解析（只认预设名/已存目录名）；login 存的是推导名（www.taobao.com → 存为 sites/taobao/），其他命令不做同样推导导致对不上——真机 `doctor www.taobao.com` 报"未知站点"，且文案误导用户用 --url（非 login 命令用 --url 无意义）。
- **影响**：非 login 四命令与 login 同用 `sites.parse_target` 推导站点名，贴 URL/域名与预设名/已存名全通；login 两档逻辑不变（自动档/回车档），底层 meta 重建只剩一套实现。文件行数 cli.py 357、test_cli.py 420，均 <500（红线 #26）。
- **验证**：全量 `.venv/bin/python -m pytest -q` = **97 passed**（含并行 T6 的 session.py 两级验活改动同树合跑；T5 自身 87 基线零回归 + 新增 7 个 + T6 新增 3 个）。真机复现命令改前 exit 1（"未知站点…请用 --url"）、改后正常解析并走真浏览器验活。未执行任何 git 操作。
- **备注**：工作期间并行任务 T6（doctor 两级验活）修改了 `pageplay/session.py` 与 `tests/test_session.py`，T5 未触碰该两文件，合树后全量绿。
