# T7a coder：recipes 纯数据层

日期：2026-09-23。负责：coder（T7a，v0.2 pick/run 第一步）。基线：git 5379c3a。

## 本次改动

| 文件 | 改动 | 为什么 |
|------|------|--------|
| `pageplay/recipes.py`（新建，103 行） | ① 常量 `REQUIRED_FIELDS`（L12：version/name/site/url/action/selector）、`_ACTIONS`（L13）、`_LIST_KEYS`（L14）、`_RECIPES_DIRNAME`（L15）；② `_recipe_path`（L18）：拼 `<site_dir>/recipes/<name>.json` 前做名字安全校验，拒收空名、"."、含 `/` `\` `..` 的名字（recipe 名直接拼路径，不校验会被 "../x" 写穿 recipes 目录）；③ `save_recipe`（L33）：必填字段缺失/None/空白串 → ValueError 点名缺哪些（L37-45）；action 非法 → ValueError（L46-48）；created_at 空缺自动补 ISO 秒级、已带则保留（L52-53）；同名直接覆盖且 log.info 一句（L59-60）；写入 ensure_ascii=False + indent=2（L57）；④ `load_recipe`（L66）：缺文件 FileNotFoundError，消息带"先 pageplay pick"指引 + 现有 recipe 清单（L71-77）；⑤ `list_recipes`（L82）：目录不存在 → []；按 name 排序，每条裁剪到 name/action/url/created_at 四键；单个文件损坏/非 dict → log.warning 跳过不炸清单（L92-98） | pick/run 共用的纯数据层，契约逐字实现；风格对齐 cookies.py（ISO 秒时间戳、ensure_ascii=False、FileNotFoundError 带指引）与 session.py 的 `logging.getLogger(__name__)` |
| `tests/test_recipes.py`（新建，158 行） | 6 组 32 用例，全 tmp_path：TestSaveLoad（往返一致+mkdir parents、中文不转义、created_at 已带保留/空串重生成）、TestValidation（六个必填字段逐一删/逐一空串 parametrize → ValueError 且不落盘、非法 action）、TestOverwrite（覆盖后新内容+新 created_at 生效且不并存、caplog 钉住"覆盖"info 日志）、TestLoadMissing（消息含"先 pageplay pick"、列出现有 recipe 名）、TestList（按 name 排序、四键裁剪、目录缺失/空 → []）、TestNameSafety（".." "." "a/b" "a\b" "x..y" "../evil" "" → ValueError 且 recipes 目录一个字节没写出、load 同样拒） | 契约逐条钉死，供 pick（T7b）与 run 回归 |

未触碰任何既有文件；未执行任何 git 操作。

## 并行任务说明

收尾时工作树出现并行产物：`pageplay/actions.py`、`tests/test_actions.py`、`tests/conftest.py` 改动（非本任务所动，开工时 status 干净）。全量 138 passed 含这些并行用例（基线实际 106 + 本任务 32 = 138），互相不冲突。任务书里"97 passed"为旧数。

## 验收数据

- `tests/test_recipes.py` 单文件：**32 passed in 0.11s**
- 全量 `.venv/bin/python -m pytest -q`：**138 passed in 6.56s**（106 既有 + 32 新增；0 failed）
- `wc -l`：recipes.py 103 ｜ test_recipes.py 158（全部 < 500）

## Phase 状态

- [x] T7a coder：recipes 纯数据层（2026-09-23，单文件 32 passed，全量 138 passed）
