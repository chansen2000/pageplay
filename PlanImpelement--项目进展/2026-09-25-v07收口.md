# v0.7 收口：picker 拆分 + 录制接线卡片字段 + 预览触发收紧

日期：2026-09-25。基线 324 passed（当日实跑确认），收口后全量 **328 passed 连跑 2 次**（324 + 新增 4）。

## 1. picker.py 拆分（v0.7-A 方案落地）

- 新建 `pageplay/_overlay_js.py`（274 行，纯字符串资产模块，零 import）：`_OVERLAY_JS` 整体搬入，改名 `OVERLAY_JS`。用脚本抽取 + `eval` 自证**字节级一致**（10030 字符），拆分零行为变化。
- `pageplay/picker.py` 550 → **285 行**（≤400 达标）：顶部 `from ._overlay_js import OVERLAY_JS`（picker.py:25）；`overlay_js()` 签名不变，改返回资产常量（picker.py:107-109）。测试透明：test_picker 全家 38 passed 不变。

## 2. recorder.py 接线卡片字段（堵功能缺口）

- 缺口：record_session 收获步只落 action/url/selector/columns，卡片确认（list_mode=cards + fields）进不了录制 flow，重放必坏。下游 flows 校验（flows.py save_flow）与 runner（list_mode=cards → extract_cards）v0.7-A 已就绪，唯独录制端断链。
- 修法：`_enter_pick._on_confirm` 按 `result["list_mode"] == "cards"` 条件透传两键（recorder.py:211-213）；纯表格步**不带新键**（向后兼容）。docstring 同步（模块头 + `_enter_pick`）。
- 测试（tests/test_recorder.py，9 → 11）：
  - `test_record_cards_harvest_replays_to_csv`：替人按 P 框选 card_site 卡片列表确认 → 收获步含 `list_mode="cards"` 与 fields 原样 → 该步原样进 flow（仅 no 重排，同 save_flow 落盘行为）→ runner 端到端重放出 CSV（表头"订单号,价格" + 6 行）。
  - `test_record_table_harvest_step_has_no_cards_keys`：纯表格收获步键集合逐字断言无新键。

## 3. gui.py 预览触发收紧

- 现状：任何命令结束只要账本最新一条有 csv 就弹旧预览（doctor 不写账本——已核实 `_record_run` 仅收获/run/recipe 调用，cli_pick.py——所以 doctor 也弹旧的）。
- 修法（闸门 = 纯函数 + 三行 UI 接线）：
  - `_count_runs(runs_path)`（gui.py:153）：账本非空行数，读不了 → 0；
  - `should_preview(before, after)`（gui.py:162）：行数有增长才弹；
  - `App.__init__` 初始化基线（gui.py:215）、`_launch` 记基线（gui.py:310）、`_on_done` 比对后才 `_maybe_preview`（gui.py:352-353）。
  - `latest_csv_output` 等既有函数未动。
- 测试（tests/test_gui.py，21 → 23）：`should_preview` 增长/首条/无增长/回退四分支；`_count_runs` 缺失 → 0、非空行计数（尾部空行不计）。UI 三行接线（基线记录 + 闸门调用）按项目惯例不自动化（tkinter 人验收），逻辑全部收在纯函数里。
- 行数控制：gui.py 原本 500 行顶格，+19 行新逻辑通过压缩装饰性横幅注释（4 处三行横幅 → 单行）与去重 docstring（取消/退出契约在 `_cancel`/`_on_close` 详述、模块头只留指针）消化，终值 **498 行**，事实信息无删失。

## 验收证据

- 全量 pytest 连跑 2 次：`328 passed in 128.31s` / `328 passed in 126.37s`（基线 324 + 新增 4）。
- wc -l：picker.py 285 / _overlay_js.py 274 / recorder.py 313 / gui.py 498 / test_recorder.py 495 / test_gui.py 259，全表 ≤500。
- 未执行任何 git 操作。

## Phase 状态

- v0.7-A 方案之 picker 拆分 ✅（2026-09-25）
- v0.7 收口三件套 ✅（2026-09-25）：picker 拆分 / 卡片字段接线 / 预览收紧
